sudo: false

language: ruby

cache: bundler

rvm:
  - 2.0.0
  - 2.1
  - 2.2

env:
  - DB=sqlite

before_script:
  - gem install brakeman
  - bin/rake db:setup RAILS_ENV=test

script:
  - RAILS_ENV=test bin/rake --trace db:migrate test
  - brakeman -z -4
