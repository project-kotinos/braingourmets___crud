= form_for book do |f|
  - if book.errors.any?
    %ul
      - book.errors.full_messages.each do |msg|
        %li= msg

  %p
    = f.label :title
    = f.text_field :title

  .actions
    = f.submit 'Save'
